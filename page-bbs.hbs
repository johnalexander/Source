{{!< default}}
{{!-- Template for the /bbs page. In Ghost Admin, create a page with slug "bbs"
     and this template is automatically applied. --}}

{{#post}}
<main class="gh-main">
    <article class="gh-article {{post_class}}">

        {{!-- Title intentionally suppressed for BBS page — the embed and CTA are the full content --}}

        <section class="gh-content gh-canvas bbs-page-section">

            {{#unless @member}}
            {{!-- ── Non-subscriber: teaser iframe ───────────────────────── --}}
            <div class="bbs-embed-wrap">
                <iframe
                    src="https://bbs.southpole.blog/embed"
                    title="SouthPole BBS Preview"
                    frameborder="0"
                    scrolling="no"
                    class="bbs-embed-frame"
                    loading="lazy"
                ></iframe>
            </div>
            <p class="bbs-subscribe-hint">
                <a href="#/portal">Subscribe to SouthPole to get access →</a>
            </p>
            {{else}}
            {{!-- ── Subscriber: terminal preview + CTA (corrected by JS) ── --}}
            <div class="bbs-embed-wrap">
                <iframe
                    src="https://bbs.southpole.blog/embed?member=1"
                    title="SouthPole BBS"
                    frameborder="0"
                    scrolling="no"
                    class="bbs-embed-frame"
                    loading="lazy"
                ></iframe>
            </div>
            {{!-- Only shown by JS when member has no handle yet (invite path) --}}
            <div
                id="bbs-portal"
                class="bbs-member-cta"
                data-uuid="{{@member.uuid}}"
                style="display:none"
            >
                <button
                    id="bbs-invite-btn"
                    class="gh-button bbs-btn bbs-invite"
                    type="button"
                >Send my BBS invite →</button>
                <p id="bbs-invite-confirm" class="bbs-invite-confirm" style="display:none"></p>
            </div>
            {{/unless}}

        </section>

    </article>
</main>

<style>
    /* ── Embed frame ─────────────────────────────────────────────── */
    .bbs-embed-wrap {
        width: 100%;
        border-radius: 4px;
        overflow: hidden;
        border: 1px solid var(--color-border, #e5e7eb);
        margin-bottom: 16px;
    }

    .bbs-embed-frame {
        display: block;
        width: 100%;
        height: 720px;
        border: none;
    }

    @media (max-width: 540px) {
        .bbs-embed-frame {
            height: 100vw;
        }
    }

    /* ── Subscribe hint ──────────────────────────────────────────── */
    .bbs-subscribe-hint {
        text-align: center;
        font-size: 0.9rem;
        margin-top: 8px;
    }

    .bbs-subscribe-hint a {
        color: inherit;
        text-decoration: underline;
        text-underline-offset: 3px;
    }

    /* ── Member CTA ──────────────────────────────────────────────── */
    .bbs-member-cta {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 12px;
        padding: 20px 0 8px;
        text-align: center;
    }

    .bbs-btn {
        display: inline-block;
        padding: 10px 22px;
        font-size: 0.95rem;
        font-weight: 600;
        cursor: pointer;
        border-radius: 4px;
        text-decoration: none;
    }

    .bbs-invite {
        background: transparent;
        color: var(--ghost-accent-color, #15171a);
        border: 2px solid currentColor;
    }

    .bbs-invite-confirm {
        font-size: 0.875rem;
        color: var(--color-secondary-text, #6b7280);
    }
</style>

{{!-- ── JS: correct the subscriber CTA based on member-status API ── --}}
{{#unless @member}}{{else}}
<script>
(function () {
    var portal = document.getElementById('bbs-portal');
    if (!portal) return;

    var uuid = portal.dataset.uuid;
    if (!uuid) return;

    var inviteBtn = document.getElementById('bbs-invite-btn');
    var confirm   = document.getElementById('bbs-invite-confirm');

    // Check whether this member already has a BBS handle.
    // If not, reveal the portal div and show the invite button.
    fetch('https://bbs.southpole.blog/api/member-status?uuid=' + encodeURIComponent(uuid), {
        credentials: 'omit'
    })
    .then(function (r) { return r.json(); })
    .then(function (data) {
        if (data && data.hasHandle === false) {
            // No handle yet — surface the invite panel
            portal.style.display = '';
            if (inviteBtn) inviteBtn.style.display = '';
        }
        // hasHandle === true → iframe already shows [ ENTER THE BBS ]; nothing to do
    })
    .catch(function () {
        // Network error — leave everything hidden; BBS login handles the edge case
    });

    // Invite button: request a fresh welcome email
    if (inviteBtn) {
        inviteBtn.addEventListener('click', function () {
            inviteBtn.disabled = true;
            fetch('https://bbs.southpole.blog/api/resend-welcome', {
                method:  'POST',
                headers: { 'Content-Type': 'application/json' },
                body:    JSON.stringify({ uuid: uuid }),
                credentials: 'omit'
            })
            .then(function () {
                if (confirm) {
                    confirm.textContent = 'Done — check your inbox for the invite link.';
                    confirm.style.display = '';
                }
                inviteBtn.style.display = 'none';
            })
            .catch(function () {
                inviteBtn.disabled = false;
                if (confirm) {
                    confirm.textContent = 'Something went wrong. Please try again.';
                    confirm.style.display = '';
                }
            });
        });
    }
}());
</script>
{{/unless}}

{{/post}}
